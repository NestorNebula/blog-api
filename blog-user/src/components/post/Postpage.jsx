import { useOutletContext } from 'react-router-dom';
import { useData } from '../../hooks/useData';
import useFetchOptions from '../../helpers/fetchOptions';

function Postpage() {}

export default Postpage;
